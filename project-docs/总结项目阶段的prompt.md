请基于当前整个对话内容，提取项目“当前最终状态”。

不要整理历史过程。
不要记录讨论过程。
不要保留已废弃方案。
不要包含“曾经考虑过”的内容。

只保留当前最终确认并仍在使用的方案。

====================
输出协议（必须严格遵守）
====================

你必须只输出两个文件：

1. PROJECT_CONTEXT.md
2. CURRENT_TASK.md

除文件内容外：

- 不允许输出解释
- 不允许输出前言
- 不允许输出备注
- 不允许输出总结
- 不允许输出“下面是”
- 不允许输出任何额外文本

====================
输出格式（必须严格遵守）
====================

使用以下分隔符协议：

===== PROJECT_CONTEXT.md START =====

文件内容

===== PROJECT_CONTEXT.md END =====

===== CURRENT_TASK.md START =====

文件内容

===== CURRENT_TASK.md END =====

注意：

- 不允许使用 markdown 代码块
- 不允许使用反引号 fence
- 不允许输出 ```
- 不允许输出 ```markdown
- 不允许嵌套代码块
- 文件内容必须是纯 markdown 正文
- 必须保证我可以直接复制保存
- 两个文件之间不能插入解释文字
- 不允许省略内容
- 不允许使用 “...” 代替内容
- 必须输出完整内容

====================
PROJECT_CONTEXT.md
必须包含且顺序一致
====================

# 项目目标

# 当前最终技术栈

# monorepo 结构

# apps/packages 职责

# 当前目录结构

# 当前架构设计

# API 设计

# 状态管理方案

# 数据流设计

# 类型管理规范

# 工程化方案

# 当前代码规范

# 当前环境配置方案

# 已完成模块

# 当前未完成模块

# 当前重要技术决策

# 当前已知问题

# 注意事项

====================
CURRENT_TASK.md
必须包含且顺序一致
====================

# 当前开发目标

# 当前开发模块

# 当前阶段已完成内容

# 当前阶段未完成内容

# 当前阻塞问题

# 下一步开发计划

# 当前涉及的重要文件

====================
内容规则
====================

- 只输出当前最终状态
- 不要输出分析过程
- 不要输出讨论历史
- 不要输出备选方案
- 如果存在冲突，以最近一次最终确认内容为准
- 如果信息不确定，不要猜测，直接标记“未确认”
- 输出标准 markdown
- 内容适合作为新对话上下文
- 内容必须适合长期 AI 协同开发
- 必须保持结构清晰
- 必须保持可读性
